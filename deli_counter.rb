def line(array)
  if array >= 1
    counter = 1
    array.each do |name|
      puts "Welcome, #{name}. You are currently number #{counter} in line."
      counter += 1
    end
    elsif array == 0
    puts "The line is currently empty."
  else
      
  end
end

def take_a_number
  
end

def now_serving
  
end
